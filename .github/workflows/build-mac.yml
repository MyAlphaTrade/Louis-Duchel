name: Build macOS

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (ex: v4.1.0)'
        required: true
        default: 'v4.1.0'

jobs:
  build-mac:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Verify source files
        run: |
          PROJECT="OneDrive/Desktop/PROJETS APP TRADE/MyAlphatrade/AlphaTrade v4.1.0 - Claude"
          echo "=== Fichiers electron ==="
          ls -la "$PROJECT/electron/"
          echo "=== package.json ==="
          cat "$PROJECT/electron/package.json"

      - name: Install Python packages
        run: |
          PROJECT="OneDrive/Desktop/PROJETS APP TRADE/MyAlphatrade/AlphaTrade v4.1.0 - Claude"
          pip3 install --target="$PROJECT/python" -r "$PROJECT/python/requirements.txt"

      - name: Install Node dependencies
        run: |
          PROJECT="OneDrive/Desktop/PROJETS APP TRADE/MyAlphatrade/AlphaTrade v4.1.0 - Claude"
          cd "$PROJECT/electron"
          npm install

      - name: Build macOS DMG
        run: |
          PROJECT="OneDrive/Desktop/PROJETS APP TRADE/MyAlphatrade/AlphaTrade v4.1.0 - Claude"
          cd "$PROJECT/electron"
          npm run build:mac
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: Upload DMG to alphatrade-releases
        uses: softprops/action-gh-release@v2
        with:
          repository: MyAlphaTrade/alphatrade-releases
          tag_name: ${{ github.event.inputs.version || github.ref_name }}
          files: |
            OneDrive/Desktop/PROJETS APP TRADE/MyAlphatrade/AlphaTrade v4.1.0 - Claude/dist/*.dmg
            OneDrive/Desktop/PROJETS APP TRADE/MyAlphatrade/AlphaTrade v4.1.0 - Claude/dist/*.dmg.blockmap
            OneDrive/Desktop/PROJETS APP TRADE/MyAlphatrade/AlphaTrade v4.1.0 - Claude/dist/latest-mac.yml
          token: ${{ secrets.RELEASES_TOKEN }}
          fail_on_unmatched_files: false
